services:
  - type: web
    name: miniatureleveltokenizationapp
    runtime: python
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn web:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
